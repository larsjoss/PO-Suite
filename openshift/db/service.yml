apiVersion: v1
kind: Service
metadata:
  name: posuite-db
  labels:
    app: posuite
    component: db
spec:
  selector:
    app: posuite
    component: db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
